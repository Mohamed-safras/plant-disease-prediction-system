import { colors } from "./colors";

export const dark = {
  background: colors.darkBackground,
  color: colors.colorWhite,
  predictionColor: colors.colorWhite,
};

export const light = {
  background: colors.colorWhite,
  color: colors.colorBlack,
  predictionColor: colors.colorGreen,
};
